import { defineAction } from '@/features/genesis-core/blueprints';

export default defineAction({
  name: 'Multiattack',
  description: 'The elemental makes two touch attacks.',
  type: 'utility',
  slug: 'fire-elemental-multiattack',
});
