import { defineAction } from '../../../features/genesis-core/blueprints';

export default defineAction({
  name: 'Water Breathing',
  description: 'The quipper can breathe only underwater.',
  type: 'ability',
  slug: 'quipper-water-breathing',
});
