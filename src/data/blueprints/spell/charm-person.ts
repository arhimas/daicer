import { defineSpell } from '../../../features/genesis-core/blueprints';

export default defineSpell({
  slug: 'charm-person',
  name: 'Charm Person',
  level: 1,
  school: 'Enchantment',
  tags: ['bard', 'druid', 'sorcerer', 'warlock', 'wizard', 'lore'],
});
