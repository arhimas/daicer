import { defineSpell } from '@/features/genesis-core/blueprints';

export default defineSpell({
  slug: 'detect-evil-and-good',
  name: 'Detect Evil and Good',
  level: 1,
  school: 'Divination',
  tags: ['cleric', 'paladin', 'lore'],
});
