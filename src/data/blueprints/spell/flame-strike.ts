import { defineSpell } from '../../../features/genesis-core/blueprints';

export default defineSpell({
  slug: 'flame-strike',
  name: 'Flame Strike',
  level: 5,
  school: 'Evocation',
  tags: ['cleric', 'devotion', 'fiend'],
});
