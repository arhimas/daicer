import { defineSpell } from '@/features/genesis-core/blueprints';

export default defineSpell({
  slug: 'locate-object',
  name: 'Locate Object',
  level: 2,
  school: 'Divination',
  tags: ['bard', 'cleric', 'druid', 'paladin', 'ranger', 'wizard', 'lore'],
});
