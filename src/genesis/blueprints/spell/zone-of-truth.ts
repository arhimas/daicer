import { defineSpell } from '@/features/genesis-core/blueprints';

export default defineSpell({
  slug: 'zone-of-truth',
  name: 'Zone of Truth',
  level: 2,
  school: 'Enchantment',
  tags: ['bard', 'cleric', 'paladin', 'lore', 'devotion'],
});
