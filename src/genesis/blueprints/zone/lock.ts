import { defineEntityZone } from '@/features/genesis-core/blueprints';

export default defineEntityZone({
  name: 'Lock',
  slug: 'lock',
  symbol: 'L',
  color: '#F39C12',
  description: 'Color palette definition for Lock',
  category: 'Item'
});
