import { defineEntityZone } from '@/features/genesis-core/blueprints';

export default defineEntityZone({
  name: 'Text',
  slug: 'text',
  symbol: 't',
  color: '#17202A',
  description: 'Color palette definition for Text',
  category: 'Item'
});
